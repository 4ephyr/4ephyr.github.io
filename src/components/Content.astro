---
const { heading } = Astro.props;
---

<section class="px-8 py-12">
    <div class="">
        <div class="flex flex-col">
            <h1 class="text-secondary font-primary text-4xl font-semibold underline dark:text-primary">{ heading }</h1>
           <slot />
        </div>
    </div>
</section>